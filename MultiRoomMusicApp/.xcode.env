# This `.xcode.env` file is versioned and is used to source the environment
# used when running script phases inside Xcode.
# To customize your local environment variables, you can make a copy of this file called
# `.xcode.env.local` which will be sourced instead of this file.

export NODE_BINARY=node

# Ensure Metro can find and connect to the React Native bundler
export RCT_METRO_PORT=8081

# Enable Metro debugging and live reload
export FORCE_BUNDLING=1

# Ensure proper environment for React Native builds
export NODE_ENV=development